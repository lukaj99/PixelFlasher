"""PixelFlasher MCP server plugin package."""
from __future__ import annotations

__version__ = "0.1.0"

# NOTE: Imports are intentionally lazy. headless_runtime triggers the wx stub and
# runtime bootstrap, so callers must import it explicitly when they are ready.
